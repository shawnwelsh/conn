# scratch-probe

Throwaway Claude Code project whose only purpose is firing hooks at the probe
server (`node scripts/probe-hooks.mjs` from the repo root). Its project-scoped
`.claude/settings.json` mirrors the exact hook block destined for
`~/.claude/settings.json`, so probe results are representative.
