# Short blurbs

Reusable copy for the GitHub "About" box, social previews, and link posts.

**GitHub About (≤ 350 chars):**
```
Take the conn over your Claude Code agents. A Stream Deck (or browser) command
surface: every session on a key, permission prompts you answer with one press,
plan approvals, voice, and per-session git worktrees. Fail-open by design.
Windows · MIT.
```

**One-liner:**
```
Conn — a Stream Deck command surface for a room full of Claude Code agents.
```

**Elevator pitch (2 sentences):**
```
Conn puts every Claude Code session on its own key: you see what each one is
doing and answer its permission prompts with a single press, while the
on-screen dialog waits. It runs in the browser (no hardware needed) and fails
open — it never turns its own failure into an automatic "allow".
```

**Repo topics (GitHub):**
```
claude-code · stream-deck · elgato · ai-agents · developer-tools · windows ·
permissions · automation · typescript
```
